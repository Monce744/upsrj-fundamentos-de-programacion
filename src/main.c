#include <stdio.h>

int main()
{
    int x = 100;
    int z= 50;
    char letra_a = 'a';
    float y = 1.1;
    float suma = x + (int)y;  
  printf("x: %d \ny: %f \nsuma: %f\n", x, y, suma);
    return 0;
}